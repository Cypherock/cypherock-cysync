{
  "name": "playground",
  "private": true,
  "source": "index.js",
  "scripts": {
    "build": "pnpm clean && pnpm build:dirty",
    "build:dirty": "PARCEL_WORKERS=0 parcel build --no-cache --no-source-maps",
    "clean": "rimraf dist"
  }
}
