import { onboarding } from './onboarding';

export const routes = {
  onboarding,
  walletDetails: {
    name: 'portfolio',
    path: '/portfolio', // TODO: change to '/'
  },
};

export * from './types';
