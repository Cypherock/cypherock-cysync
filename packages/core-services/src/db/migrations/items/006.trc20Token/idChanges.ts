const data = {
  idChanges: [
    {
      oldId: 'wrapped-btt',
      newId: 'wrapped-btt-tron',
      platform: 'tron',
    },
  ],
  changedCoins: [
    {
      id: 'wrapped-btt-tron',
      platform: 'tron',
    },
  ],
};

export const { idChanges } = data;
export const { changedCoins } = data;
